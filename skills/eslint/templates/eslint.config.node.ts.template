// eslint.config.ts — Node.js backend (no React/JSX)
// Drop into project root. Requires:
//   npm i -D eslint @eslint/js typescript-eslint eslint-config-prettier globals

import js from "@eslint/js";
import tseslint from "typescript-eslint";
import prettier from "eslint-config-prettier";
import globals from "globals";

export default tseslint.config(
  // 1. Global ignores
  {
    ignores: ["dist/**", "build/**", "coverage/**", "node_modules/**", "*.config.js"],
  },

  // 2. JS recommended
  js.configs.recommended,

  // 3. TS recommended (type-checked)
  ...tseslint.configs.recommendedTypeChecked,

  // 4. TS parser options
  {
    files: ["**/*.{ts,cts,mts}"],
    languageOptions: {
      parserOptions: {
        projectService: true,
        tsconfigRootDir: import.meta.dirname,
      },
      globals: {
        ...globals.node,
      },
    },
  },

  // 5. Node-specific rules
  {
    rules: {
      // Catch unhandled promises in backend code (high-value)
      "@typescript-eslint/no-floating-promises": "error",
      "@typescript-eslint/no-misused-promises": "error",
      "@typescript-eslint/await-thenable": "error",
      "@typescript-eslint/require-await": "error",

      // No console in production code; pino/winston instead
      "no-console": "warn",
      "no-debugger": "error",

      // Hygiene
      "@typescript-eslint/no-unused-vars": [
        "error",
        { argsIgnorePattern: "^_", varsIgnorePattern: "^_", caughtErrorsIgnorePattern: "^_" },
      ],
      "@typescript-eslint/consistent-type-imports": [
        "error",
        { prefer: "type-imports", fixStyle: "inline-type-imports" },
      ],
      "@typescript-eslint/no-explicit-any": "warn",

      // Module hygiene
      "no-restricted-imports": [
        "error",
        {
          patterns: [{ group: ["lodash"], message: "Import named modules from lodash-es" }],
        },
      ],
    },
  },

  // 6. Test files — relaxed
  {
    files: ["**/*.{test,spec}.ts", "**/__tests__/**/*.ts"],
    rules: {
      "@typescript-eslint/no-explicit-any": "off",
      "@typescript-eslint/no-non-null-assertion": "off",
      "no-console": "off",
    },
  },

  // 7. Prettier — MUST be last
  prettier,
);
