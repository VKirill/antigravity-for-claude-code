# Чек дебатов (Debate Receipt)

### 1. Тема обсуждения
Обсуждение было посвящено двум взаимосвязанным архитекту��ным вопросам:
1. **Миграция среднеразмерного e-commerce проекта с Next.js Pages Router на App Router (Next.js 16)** для оптимизации ключевых метрик Core Web Vitals (в первую очередь LCP и INP). Рассматривалось использование пр��имуществ React Server Components (RSC), Partial Prerendering (PPR), новой директивы `'use cache'` и Server Actions.
2. **Внедрение автоматического проверочного контура качества (Site-Harness Pipeline)** для предотвращения ге��ерации «нейрошлака» (шаблонных, визуально скучных и некачественных интерфейсов, создаваемых большими языковыми моделями). Обсуждалась строгость проверок на уровне AST-анализа исходного кода, аудит собранного ��андла, автоматические откаты изменений в Git при сбоях и использование локальной базы данных для управления фазами пайплайна.

---

### 2. Сводная таблиц�� участников

| Участник (Роль) | Основной тезис (Claim) | Аргументы и доказательства (Evidence) |
| :--- | :--- | :--- |
| **Оптимист (Optimist)** | Миграция на Next.js 16 App Router необходима и должна проходить поэтапно. Автоматизированный контур проверок гарантирует строгое соблюдение премиальных ��тандартов визуального качества. | Использование React Server Components (RSC) и Partial Prerendering (PPR) позволяет отда��ать статический каркас страницы мгновенно, а динамические данные (цены, остатки) стримить в параллельном режиме. Директива `'use cache'` с тегами (`cacheTag`) и ревалидацией по событиям из PIM/CMS обеспечивает эффективное кэширование данных. AST-линтер (`craft-gate.mjs`) на этапе сборки принудительно запрещает использование HEX/RGB/HSL, требуя применять исключительно OKLCH-цвета, fluid clamp-функции и CSS Grid. |
| **Скептик (Skeptic)** | Текущая архитектура проверочного контура содержит серьезные уязвимости, ломает историю Git, создает инфраструктурные риски и предлагает слишком хрупкие гейты. | Git-команда `git reset --soft HEAD~1` затирает реальную историю коммитов разработчика, если репозиторий изначально был чистым. Передача больших YAML-контрактов че��ез CLI-аргументы уязвима к командным инъекциям и может упасть из-за ограничения `ARG_MAX`. Статический поиск подстрок (например, `display: grid` или `gsap`) в скомпилированном бандле `dist/` хрупок: оптимизаторы и сборщики (PostCSS/Vite) сжимают код или транспилируют OKLCH-цвета обратно в HEX для старых браузеров, что вызывает ложные срабатывания линтера. Использование SQLite (`tasks.db`) для локального отслеживания фаз создаст конфликты при параллельной работе над ветками. |
| **Соглашатель (Agreer)** | Архитектурные возможности Next.js 16 полезны, но построение сложного контура качества (AST-линтеры, базы данн��х, Git-откаты) — это избыточность, котор��я только замедляет разработку. | Строгие Zod-схемы и принудительные проверки со��дают лишний бюрократический шум. Намного быстрее использовать стандартный Tailwind, простые JSON-объекты и проводить визуальную проверку страницы в браузере за 5 секу��д перед деплоем, а не писать и поддерживать PostCSS-валидаторы. |
| **Хейтер (Hater)** | Идея автоматических качественных гейтов — дорогостоящий способ сжигать бюджет на API-токены, который приведет лишь к имитации качества и «театру тестов». | **Закон Гудхарта**: модель быстро научится обходить AST-линтер, генерируя мусорный, неработающий CSS (например, фиктивные невидимые сетки или бесполезные функции `clamp()`), который формально ��ройдет тесты, но сломает реальный интерфейс. Механизм `git rollback` при любой ошибке валидации заведет агента в бесконечный цикл дорогостоящи�� правок из-за мелких опечаток. Ручное подтверждение (Human sign-off) лишает ��роцесс автономии. Исторические примеры: Knight Capital (крах из-за сбоя ПО в 2012) и практика написания пустых заглушек для достижения 100% покр��тия тестами. |

---

### 3. Отвергнутые альтернативы
В ходе обсуждения были ��редложены и отклонены следующие варианты решений:

1. **Пол��ый отказ от автоматических проверок кода в пользу ручного визуального ��онтроля (предложение Соглашателя):**
   * *Причина отклонения:* Без жестких автоматизированных проверок языковые модели неизбежно скатываются к генерации плоских, шаблонных макетов и игнорируют требования к ка��еству кода, стилю и доступности.
2. **Полный отказ от идеи создан��я проверочного контура качества (позиция Хейтера):**
   * *Причина отклонения:* В e-commerce проекте критически важно поддерживать строгие визуальные стандарты; о��каз от автоматизации проверки приведет к быстрой деградации код��вой базы.
3. **Жесткий откат изменений (`git reset --hard`) при первой же ошибке ва��идации:**
   * *Причина отклонения:* Этот подход уничтожал до 95% корректно выполненной работы агента из-за мелкой опечатки, приводил к потере локальных данных и мешал параллельной работе над д��угими задачами.
4. **Проведение всех проверок исключительно на уровне собранно��о бандла (`dist/`):**
   * *Причина отклонения:* Оптимизаторы и сборщики (PostCSS/Vite) сжимают код и транспилируют современные свойства (например, переводят `oklch` обратно в HEX/RGB для совместимос��и со старыми браузерами), что приводило к ложным срабатываниям линтера.
5. **Замена базы данных SQLite на плоский файл сос��ояния `.harness/state.json`:**
   * *Причина отклонения:* Для надежной оркестрации фаз выполнения и предотвращения конфликтов в многопоточном режи��е необходима транзакционность, которую плоский файл состояния обеспечить не может.

---

### 4. Итоговое принятое решение
На основе ко��промиссов, предложенных Синтезатором (ведущим архитектором), утверждено след��ющее решение:

1. **Поэтапная миграция на Next.js 16 App Router одобрена:** Перехо�� будет реализован в 5 фаз (от простых статических страниц до корзины и чекаута с поэтапным внедрением PPR, Server Actions и `'use cache'`).
2. **Модернизация контура провер��к (Quality Harness):**
   * **Переход на мягкие гейты (Soft Gates):** Вместо немедленного жесткого отката изменений агент получает до 3 попыток исправить код прямо на месте (in-place) на основе логов ошибок компилятора или линтера.
   * **Двухпроходной аудит (Dual-Pass Audit):** Проверка разделена на два ш��га:
     - *Pass 1 (AST-анализ на уровне исходного кода в `src/`):* Контроль использования OKLCH-ц��етов, fluid clamp и дизайн-токенов.
     - *Pass 2 (Аудит собранного бандла в `dist/`):* Проверка наличия CSS Grid, импорта GSAP, ани��аций и поддержки `prefers-reduced-motion`.
   * **Борьба с «театром соответствия»:** Внедряется визуальное тестирование (Playwright + Screenshot Diff) для проверки реального рендеринга страницы вместо ��лепого поиска подстрок в коде.
   * **Изоляция рабочих пространств:** Для параллельной работы агентов будут использоваться изолированные `git worktrees` или контейнеры, что исключает конфликт�� глобальных откатов.
   * **Визуальные гейты (Human Concept Gates):** Сложные дизайн-фазы (например, `art-direction`) блокирую��ся до ручного одобрения человеком сгенерированных ИИ макетов (фреймов), предотвращая написание кода под неверную концепцию.
   * **Оптимизация БД:** База SQLite переведена в режим WAL (Write-Ahead Logging) для предотвращения взаимных блокировок.

## Аудит безопасности и изменений (Hooks Audit)

*Изменений файлов в рамках этой сессии зафиксировано не было.*

> [!NOTE]
> Попыток нарушения правил кодирования (использование `@ts-ignore` или жестко заданных цветов) в этой сессии не зафиксировано.

